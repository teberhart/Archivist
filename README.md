# Archivist

Archivist is a Next.js project. The app lives in the `archivist/` subfolder.

## Getting Started

Install dependencies and run the dev server:

```bash
cd archivist
pnpm install
pnpm dev
```

Open http://localhost:3000 to view the app.
